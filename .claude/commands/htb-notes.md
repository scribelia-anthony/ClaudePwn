Ajouter une note manuelle à la box active.

Argument : $ARGUMENTS (le texte de la note)

Lis `.claudepwn-active` pour la box.
Ajoute le texte à la section "## Notes" de `boxes/{box}/notes.md` avec un timestamp :

```
- [HH:MM] {texte de la note}
```

Confirme l'ajout en une ligne.
