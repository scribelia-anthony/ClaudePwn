L'utilisateur te pose une question. C'est le moment de parler.

Question : $ARGUMENTS

Lis `.claudepwn-active` pour la box et l'IP.
Lis `boxes/{box}/notes.md` pour le contexte complet.
Lis les scans pertinents dans `boxes/{box}/scans/` si nécessaire.

Réponds à la question de manière :
- **Détaillée et technique** — donne une vraie analyse
- **Contextuelle** — utilise les données de la box (ports, services, versions, credentials trouvés)
- **Actionnable** — propose des commandes concrètes à essayer
- **Honnête** — si tu ne sais pas, dis-le

C'est le seul moment où tu as le droit de faire des paragraphes et d'expliquer.
